import assetbinds_new_asset from "../../../public/images/assetbinds_new_asset.png";
import assetbinds_new_gamepass from "../../../public/images/assetbinds_new_gamepass.png";
import assetbinds_new_badge from "../../../public/images/assetbinds_new_badge.png";
import assetbinds_modify_template from "../../../public/images/assetbinds_modify_template.png";
import assetbinds_modify_priority from "../../../public/images/assetbinds_modify_priority.png";
import assetbinds_modify_add_roles from "../../../public/images/assetbinds_modify_add_roles.png";
import assetbinds_modify_remove_roles from "../../../public/images/assetbinds_modify_remove_roles.png";
import assetbinds_delete from "../../../public/images/assetbinds_delete.png";
import assetbinds_delete_multiple from "../../../public/images/assetbinds_delete_multiple.png";
import Image from "next/image";

# Assetbinds

Asset are a type of bind that are used to add roles or nickname any user who owns a particular asset, badge or gamepass.

## Creating a Assetbind

Assetbinds are of three types: `Asset`, `Badge` or `Gamepass`.

### Creating a Assetbind for Assets

To create a assetbind, run the command `/assetbinds new` and set the `option` argument to `Asset`.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-2 md:py-4 xl:py-8 px-1 md:px-4 xl:px-6 rounded-sm">
  <Image
    src={assetbinds_new_asset}
    alt="Assetbinds New Asset"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>

It requires 3 arguments and has 2 optional arguments.

- **Asset Id**: This is the ID of your Asset.
- **Template**: Learn more about it [here](/docs/template).

#### Optional Arguments:

- **Priority**: Learn more about it [here](/docs/priority).
- **Discord Roles**: This is a list of Discord Roles that are to be added to the bind.

### Creating a Assetbind for Gamepasses

To create a assetbind, run the command `/assetbinds new` and set the `option` argument to `Gamepass`.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-2 md:py-4 xl:py-8 px-1 md:px-4 xl:px-6 rounded-sm">
  <Image
    src={assetbinds_new_gamepass}
    alt="Assetbinds New Gamepass"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>

### Creating a Assetbind for Badges

To create a assetbind, run the command `/assetbinds new` and set the `option` argument to `Badge`.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-2 md:py-4 xl:py-8 px-1 md:px-4 xl:px-6 rounded-sm">
  <Image
    src={assetbinds_new_badge}
    alt="Assetbinds New Badge"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>

## Viewing your Assetbinds

To view all your assetbinds, run the command `/assetbinds view`.

## Modifying a Assetbind

### Modifying the template

To modify the template of an assetbind, use the `/assetbinds modify template` command. Learn more about templates [here](/docs/template).

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={assetbinds_modify_template}
    alt="Assetbinds Modify Template"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>

### Modifying the priority

To modify the priority of an assetbind, use the `/assetbinds modify priority` command. Learn more about priority [here](/docs/priority).

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={assetbinds_modify_priority}
    alt="Assetbinds Modify Priority"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>

### Adding Discord Roles

To add additional Discord roles to an assetbind, use the `/assetbinds modify add-roles` command

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={assetbinds_modify_add_roles}
    alt="Assetbinds Modify Add Roles"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>

### Removing Discord Roles

To remove existing Discord roles from a groupbind, use the `/groupbinds modify remove-roles` command.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={assetbinds_modify_remove_roles}
    alt="Assetbinds Modify Remove Roles"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>

## Deleting an assetbind

To delete a single assetbind, run the `/assetbinds delete` command.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={assetbinds_delete}
    alt="Assetbinds Delete"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>

## Deleting Multiple Assetbinds

To delete multiple assetbinds, run the `/assetbinds delete` command. The asset IDs should be entered as space separated numbers
as shown below.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={assetbinds_delete_multiple}
    alt="Assetbinds Multiple Delete"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>
