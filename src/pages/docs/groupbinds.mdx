import groupbinds_new from "../../../public/images/groupbinds_new.png";
import groupbinds_modify_template from "../../../public/images/groupbinds_modify_template.png";
import groupbinds_modify_priority from "../../../public/images/groupbinds_modify_priority.png";
import groupbinds_modify_add_roles from "../../../public/images/groupbinds_modify_add_roles.png";
import groupbinds_modify_remove_roles from "../../../public/images/groupbinds_modify_remove_roles.png";
import groupbinds_delete from "../../../public/images/groupbinds_delete.png";
import groupbinds_delete_multiple from "../../../public/images/groupbinds_delete_multiple.png";
import Image from "next/image";

# Groupbinds

Groupbinds are a type of bind that are used to add roles or nickname any user who is present in a Roblox Group regardless
of their rank.

## Creating a Groupbind

To create a groupbind, run the command `/groupbinds new`.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-2 md:py-4 xl:py-8 px-1 md:px-4 xl:px-6 rounded-sm">
  <Image
    src={groupbinds_new}
    alt="Groupbinds New"
    className="rounded-md shadow-xl"
    placeholder="blur"
  />
</div>

It requires 3 arguments and has 2 optional arguments.

- **Group Id**: This is the ID of your Roblox Group.
- **Template**: Learn more about it [here](/docs/template).

#### Optional Arguments:

- **Priority**: Learn more about it [here](/docs/priority).
- **Discord Roles**: This is a list of Discord Roles that are to be added to the bind.

## Viewing your Groupbinds

To view all your groupbinds, run the command `/groupbinds view`.

## Modifying a Groupbind

### Modifying the template

To modify the template of a groupbind, use the `/groupbind modify template` command. Learn more about templates [here](/docs/template).

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={groupbinds_modify_template}
    alt="Groupbinds Modify Template"
    className="rounded-md shadow-xl"
  />
</div>

### Modifying the priority

To modify the priority of a groupbind, use the `/groupbinds modify priority` command. Learn more about priority [here](/docs/priority).

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={groupbinds_modify_priority}
    alt="Groupbinds Modify Priority"
    className="rounded-md shadow-xl"
  />
</div>

### Adding Discord Roles

To add additional Discord roles a groupbind, use the `/groupbinds modify add-roles` command

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={groupbinds_modify_add_roles}
    alt="Groupbinds Modify Add Roles"
    className="rounded-md shadow-xl"
  />
</div>

### Removing Discord Roles

To remove existing Discord roles from a groupbind, use the `/groupbinds modify remove-roles` command.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={groupbinds_modify_remove_roles}
    alt="Groupbinds Modify Remove Roles"
    className="rounded-md shadow-xl"
  />
</div>

## Deleting a groupbind

To delete a single groupbind, run the `/groupbinds delete` command.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={groupbinds_delete}
    alt="Groupbinds Delete"
    className="rounded-md shadow-xl"
  />
</div>

## Deleting Multiple Groupbinds

To delete multiple groupbinds, run the `/groupbinds delete` command. The group IDs should be entered as space separated numbers
as shown below.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={groupbinds_delete_multiple}
    alt="Groupbinds Multiple Delete"
    className="rounded-md shadow-xl"
  />
</div>
