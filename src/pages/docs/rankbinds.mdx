import rankbindsNewSingle from "../../../public/images/rankbinds_single_new.png";
import rankbindsModifyTemplate from "../../../public/images/rankbinds_modify_template.png";
import rankbindsModifyPriority from "../../../public/images/rankbinds_modify_priority.png";
import rankbindsModifyAddRoles from "../../../public/images/rankbinds_modify_add_roles.png";
import rankbindsModifyRemoveRoles from "../../../public/images/rankbinds_modify_remove_roles.png";
import rankbindsDelete from "../../../public/images/rankbinds_delete.png";
import rankbindsMultipleDelete from "../../../public/images/rankbinds_delete_multiple.png";
import Image from "next/image";

# Rankbinds

Rankbinds are a type of bind that connect a rank in your Roblox group to a role in your Discord server.

## Creating a Rankbind

To create a single rankbind, run the command `/rankbinds new`.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={rankbindsNewSingle}
    alt="Rankbinds New Single"
    className="rounded-md shadow-xl"
  />
</div>

It requires 3 arguments and has 2 optional arguments.

- **Group Id**: This is the ID of your Roblox Group.
- **Rank Id**: This is the ID of the rank from your Roblox Group that you wish to bind. It is from 1-255.
- **Template**: Learn more about it [here](/docs/template).

#### Optional Arguments:

- **Priority**: Learn more about it [here](/docs/priority).
- **Discord Roles**: This is a list of Discord Roles that are to be added to the bind.

## Viewing your Rankbinds

To view all your rankbinds, run the command `/rankbinds view`.

## Modifying a Rankbind

### Modifying the template

To modify the template of a rankbind, use the `/rankbinds modify template` command. Learn more about templates [here](/docs/template).

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={rankbindsModifyTemplate}
    alt="Rankbinds Modify Template"
    className="rounded-md shadow-xl"
  />
</div>

### Modifying the priority

To modify the priority of a rankbind, use the `/rankbinds modify priority` command. Learn more about priority [here](/docs/priority).

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={rankbindsModifyPriority}
    alt="Rankbinds Modify Priority"
    className="rounded-md shadow-xl"
  />
</div>

### Adding Discord Roles

To add additional Discord roles a rankbind, use the `/rankbinds modify add-roles` command

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={rankbindsModifyAddRoles}
    alt="Rankbinds Modify Add Roles"
    className="rounded-md shadow-xl"
  />
</div>

### Removing Discord Roles

To remove existing Discord roles from a rankbind, use the `/rankbinds modify remove-roles` command.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={rankbindsModifyRemoveRoles}
    alt="Rankbinds Modify Remove Roles"
    className="rounded-md shadow-xl"
  />
</div>

## Deleting a Rankbind

To delete a single rankbind, run the `/rankbinds delete` command.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={rankbindsDelete}
    alt="Rankbinds Delete"
    className="rounded-md shadow-xl"
  />
</div>

## Deleting Multiple Rankbinds

To delete multiple rankbinds, run the `/rankbinds delete` command. The rank id argument should then be entered as space separated
numbers or a range of numbers or both as shown in the example below.

<div className="bg-gradient-to-r from-blue-400 to-pink-500 flex py-8 px-6 rounded-sm">
  <Image
    src={rankbindsMultipleDelete}
    alt="Rankbinds Multiple Delete"
    className="rounded-md shadow-xl"
  />
</div>
